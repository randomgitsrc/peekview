- [1-read-docs] 读取 P2-design.md + P1-requirements.md + review-roles + 现有代码文件（useMarkdown.ts, MarkdownViewer.vue, EntryDetailView.vue, types, entry store, mime.ts, html_render_service.py, files.py）
- [2-bdd-coverage] 检查 16 条 BDD 覆盖矩阵：P2-design §4 列出 16 条全部映射到方案机制，P0(7)+P1(6)+P2(2)+P3(1 skip)=16。AC-P0-1~7, AC-P1-1~6, AC-P2-1~2 全覆盖
- [3-pathmap-boundaries] 检查 pathMap 边界：同名文件→删除key(安全)；绝对路径→提取basename(P0-3 覆盖)；./前缀→while循环strip；外部URL→normalizeRef返回null跳过；空字符串→null返回。buildPathMap priority系统：path(1)>filename(2)>basename(3)，同priority冲突→删除。发现1个ISSUE：buildPathMap中同priority比较逻辑有bug——现有代码只比较priority<existing时覆盖，但existing存的是file.id(number)而非priority，需修复
- [4-md-rules-compat] 检查 markdown-it rules 覆写：现有 useMarkdown.ts 用 fence/heading_open 同模式覆写+恢复，image/link_open 覆写方式一致。覆写在 md.render() 前设置，render后恢复。与现有管线兼容。PASS
- [5-dom-walk-timing] 检查 DOM walk 时机：设计明确在 DOMPurify sanitize 之后执行（§7 渲染管线步骤3→4），每个 html block 独立 parse+walk。性能：实际场景<100KB可接受。但发现1个CONCERN：rewriteHtmlRefs 对每个 html block 都 new DOMParser().parseFromString，而 blocks 数组可能有很多小 segment（CODE_BLOCK 分割后），频繁 DOMParser 实例化有开销。建议：可合并相邻 html blocks 后一次性 walk，或接受当前设计（实际影响小）。非 BLOCKER
- [6-vue-coordination] 检查 Vue 组件协调：MarkdownViewer 新增 navigate-file emit + handleLinkClick 事件委托，与现有 handleCodeBlockCopy 模式一致。EntryDetailView 新增 handleNavigateFile 调用 entryStore.selectFile。selectFile 签名接受 File 对象（stores/entry.ts:100），handleNavigateFile 通过 files.find 查找 File 对象后传入。协调正确。PASS
- [7-inject-resources-ext] 检查 P2 后端扩展：inject_resources 新增 all_files+slug 参数，复用 _sibling_keys 生成 key。但 AllFileRef dataclass 只有 file_id/filename/path，而 _sibling_keys 接受 SiblingFileData（含 content/language/is_binary 等）。设计说'复用现有 key 生成逻辑'，但 _sibling_keys 签名不兼容 AllFileRef。需提取 key 生成逻辑为独立函数或让 AllFileRef 适配。CONCERN：设计伪代码直接调用 _sibling_keys(f) 但 f 是 AllFileRef 类型，类型不匹配。非 BLOCKER 但 P4 实现时需解决
- [8-alternatives-weighing] 检查候选方案权衡：方案A(前端重写) vs B(后端render路由) vs C(MCP预处理)。方案B致命障碍(Python无markdown-it+Shiki)分析充分。方案C不能覆盖所有发布路径分析正确。方案A选择理由充分（可行性+最小改动+安全性+渐进实现）。方案B作为P2长期方向、C作为可选增强的定位合理。PASS
- [9-gate-commands] 检查 gate_commands 可执行性：P5/P6 用 pytest + vitest，P8 用 grep CHANGELOG。命令格式正确。但 P5/P6 缺少 cd backend && 的前缀一致性（前端命令没 cd frontend-v3）。实际命令是两条用 ; 连接，前端命令隐式需要当前目录。CONCERN：命令应在各自目录执行。非 BLOCKER，实现时注意即可
- [10-scoring-dimensions] 评分维度评估：(1)交互状态覆盖率 8/10 — loading/error/empty 有覆盖，但链接点击的 hover/active 状态未提及；(2)AI Slop 风险 9/10 — 设计具体到代码级别，无模糊空间；(3)移动端考虑 6/10 — 链接点击事件委托在移动端 touch 事件下行为未说明（touchstart vs click 300ms延迟）；(4)可访问性 5/10 — data-peekview-file-id 属性无 ARIA 语义，屏幕阅读器无法识别内部文件链接
- [11-normalize-ref-divergence] 前端 normalizeRef 对 / 开头的绝对路径提取 basename（支持 AC-P0-3），后端 normalize_ref 对 / 开头返回 null（_BYPASS_PREFIXES 含 /）。设计 §6 明确声明'各自合理'，理由是前端 Markdown 场景需 basename fallback，后端 HTML 场景跳过 / 路径。合理差异。但注意：后端 P2 扩展的 all_files 场景，<a href='/some/path'> 也需要 basename fallback——此时后端 normalize_ref 会跳过 / 开头，与设计意图矛盾。CONCERN：P2 后端 all_files 场景的 normalize_ref 行为需调整或用单独函数。非 P0/P1 BLOCKER（P2 实现时解决）
- [12-review-complete] 1 BLOCKER (buildPathMap priority bug), 5 CONCERN, status=needs-revision
