---
phase: P1
task_id: T083-cjk-search-fix
type: review
parent: P1-requirements.md
trace_id: T083-P1-20260731
status: approved
created: 2026-07-31
agent: requirements-review
---

# P1 需求基线评审 — T083: 中文搜索与 Tag 过滤修复（终审，retry #1）

## 评审范围

对修订后的 `P1-requirements.md`（agent: analyst, 17 条 BDD + 15 项隐含需求 + 全阶段不裁 + risk:medium）进行终审。

上一轮评审 status: needs-revision，提出 1 项必须修改（rebuild_fts_index 第三路径）+ 2 项建议修改。本轮确认修改落实情况。

## 必须修改项确认

### 1. rebuild_fts_index 第三路径（上一轮阻塞项）— 已落实 ✓

上一轮要求：隐含需求 1 应扩展覆盖 `rebuild_fts_index`（database.py:379-428）第四条 FTS 文本写入路径。

修订后 P1-requirements.md：
- **L26**：声明"共有四条独立的 FTS 文本写入路径，均需走 jieba 分词 + 连字符→空格处理"
- **L30**：逐条列出四条路径——trigger（database.py L277-302）、`_update_fts_content`（entry_service.py L68-114）、`backfill_fts_content`（database.py L506）、`rebuild_fts_index`（database.py L379-428），并加粗声明"**所有四条路径都需覆盖 jieba 分词 + 连字符→空格处理**"
- L30 还说明了 rebuild_fts_index 当前未被启动流程调用但需同步处理的理由（公开函数 + P7 一致性检查）

**判定：必须修改项已落实，阻塞解除。**

## 建议修改项确认

### 2. BDD-17 测量方式 — 已落实 ✓

上一轮建议：补充首请求响应时间的测量方式说明。

修订后 P1-requirements.md L169：BDD-17 Then 条件补充了"通过 `curl -w '%{time_total}'` 测量首请求耗时，或 pytest 超时断言 `@pytest.mark.timeout(1)` 验证"。

### 3. search_entries 函数处理策略 — 已落实 ✓

上一轮建议：声明 search_entries 函数的处理策略，避免 P4 实现时遗漏。

修订后 P1-requirements.md L72：隐含需求 15 声明"**处理策略：同步更新 `search_entries` 的查询端分词逻辑**，与 `list_entries` 保持一致，避免测试与生产行为分叉"。

## BDD 评审

- **BDD-1**（中文 tag 过滤）：PASS 可二值判定。覆盖维度：数据✓ 前端✓ 多端✓ 边界✓ 兼容✓。
- **BDD-2**（日文 tag 过滤）：PASS 可二值判定。覆盖维度：数据✓ 前端✗ 多端✓ 边界✓ 兼容✓。
- **BDD-3**（英文 tag 过滤零回归）：PASS 可二值判定。覆盖维度：数据✓ 前端✗ 多端✓ 边界✓ 兼容✓。
- **BDD-4**（tag 精确匹配不误命中子串）：PASS 可二值判定。覆盖维度：数据✓ 前端✗ 多端✓ 边界✓ 兼容✓。
- **BDD-5**（多 tag 过滤 AND 语义）：PASS 可二值判定。覆盖维度：数据✓ 前端✗ 多端✓ 边界✓ 兼容✓。
- **BDD-6**（不存在的 tag 返回空）：PASS 可二值判定。覆盖维度：数据✓ 前端✗ 多端✓ 边界✓ 兼容✓。
- **BDD-7**（中文子词搜索命中）：PASS 可二值判定。覆盖维度：数据✓ 前端✗ 多端✓ 边界✓ 兼容✓。
- **BDD-8**（中文整词搜索命中）：PASS 可二值判定。覆盖维度：数据✓ 前端✗ 多端✓ 边界✓ 兼容✓。
- **BDD-9**（英文搜索零回归）：PASS 可二值判定。覆盖维度：数据✓ 前端✗ 多端✓ 边界✓ 兼容✓。
- **BDD-10**（中英文混合搜索命中）：PASS 可二值判定。覆盖维度：数据✓ 前端✗ 多端✓ 边界✓ 兼容✓。
- **BDD-11**（无匹配中文搜索返回空）：PASS 可二值判定。覆盖维度：数据✓ 前端✗ 多端✓ 边界✓ 兼容✓。
- **BDD-12**（连字符 tag 子词搜索命中）：PASS 可二值判定。覆盖维度：数据✓ 前端✗ 多端✓ 边界✓ 兼容✓。
- **BDD-13**（连字符 tag 整词搜索命中）：PASS 可二值判定。覆盖维度：数据✓ 前端✗ 多端✓ 边界✓ 兼容✓。
- **BDD-14**（启动后存量数据 FTS 索引重建）：PASS 可二值判定。覆盖维度：数据✓ 前端✗ 多端✓ 边界✓ 兼容✓。
- **BDD-15**（新建 entry FTS 索引正确分词）：PASS 可二值判定。覆盖维度：数据✓ 前端✗ 多端✓ 边界✓ 兼容✓。
- **BDD-16**（现有测试全部通过）：PASS 可二值判定。覆盖维度：数据✓ 前端✗ 多端✓ 边界✓ 兼容✓。
- **BDD-17**（jieba 预加载不阻塞首请求）：PASS 可二值判定。覆盖维度：数据✓ 前端✗ 多端✓ 边界✓ 兼容✓。测量方式已补充。

**BDD 编号格式**：17 条全部使用 `#### BDD-NN:` 标准格式，编号 1-17 连续不跳号，每条只有一条 Given-When-Then。通过。

## 隐含需求覆盖

### 数据维度 — 覆盖✓

- 隐含需求 1（四条 FTS 写入路径不一致）：覆盖✓。已扩展为四条路径（trigger、`_update_fts_content`、`backfill_fts_content`、`rebuild_fts_index`），上一轮遗漏的 rebuild_fts_index 已补入。
- 隐含需求 2（backfill 触发条件不满足）：覆盖✓。
- 隐含需求 3（summary 和 content 也需预分词）：覆盖✓。
- 隐含需求 4（查询端也需分词）：覆盖✓。

### 前端维度 — 覆盖✓

P0-brief 明确约束不改前端。声明无隐含需求，正确。

### 多端维度 — 覆盖✓

P0-brief 明确约束不改 MCP。CLI 和 API 走后端，不直接操作 FTS。声明无隐含需求，正确。

### 边界维度 — 覆盖✓

- 隐含需求 5（jieba 并发安全）：覆盖✓。
- 隐含需求 6（jieba 首次加载延迟）：覆盖✓。
- 隐含需求 7（jieba 对非中文影响）：覆盖✓。
- 隐含需求 8（空 tags/空 query）：覆盖✓。
- 隐含需求 9（FTS5 查询净化与 jieba 交互）：覆盖✓。
- 隐含需求 10（json_each 对非数组 tags）：覆盖✓。

### 兼容维度 — 覆盖✓

- 隐含需求 11（英文 tag 过滤零回归）：覆盖✓。
- 隐含需求 12（英文搜索零回归）：覆盖✓。
- 隐含需求 13（FTS trigger 的 summary 字段）：覆盖✓。
- 隐含需求 14（现有测试可能断言 LIKE）：覆盖✓。
- 隐含需求 15（search_entries 函数处理策略）：覆盖✓。已声明同步更新策略。

## 裁剪评审

全阶段不裁（P1-P8），理由充分：
- P1 不裁：核心阶段，不可裁剪。✓
- P2 不裁：四路径协调 + jieba 并发安全 + 预加载时序 + 查询端分词语义。✓
- P3 不裁：17 条 BDD 需 TDD 红灯。✓
- P4 不裁：代码实现。✓
- P5 不裁：全量测试 + 环境隔离验证。✓
- P6 不裁：BDD 逐条实跑验收。✓
- P7 不裁：多文件改动跨文件一致性检查。✓
- P8 不裁：jieba 新依赖 + 版本/CHANGELOG 更新。✓

**risk_level: medium 判定合理**：四路径协调（机制交叉≥2 子系统交互）+ jieba 新依赖（并发安全+预加载时序）+ 存量数据迁移。不涉及 schema 变更/安全/多端，故不是 high。✓

## P1 纯净性（方案渗入检查）

**结论：无方案设计渗入。**

- 隐含需求提到 `json_each` 和 jieba 分词——这些是 P0-brief 已确认的修法方向，P1 声明的是"所有 FTS 文本写入路径都需覆盖"这一需求约束，不是新设计方案。可接受。
- BDD 条件均描述用户行为（API 请求 → 响应结果），未描述技术实现。通过。

## 覆盖维度标注总结

| 维度 | 状态 |
|------|------|
| 完整性 | 通过 — 四条 FTS 写入路径全覆盖（上一轮遗漏已修复） |
| 可验证性 | 通过 — 17 条 BDD 全部可二值判定，BDD-17 测量方式已补充 |
| 裁剪合理性 | 通过 — 全阶段不裁，理由充分，risk:medium 判定合理 |
| 方案渗入检查 | 通过 — 无方案设计渗入，BDD 描述用户行为 |

## 结论

**status: approved**

上一轮必须修改项（rebuild_fts_index 第三路径，BDD-14/BDD-15 相关的 FTS 写入路径完整性）已落实。建议修改项（BDD-17 测量方式、search_entries 处理策略）均已落实。无新问题。

BDD-1 至 BDD-17 全部可二值判定，隐含需求 1-15 覆盖数据/前端/多端/边界/兼容五个维度，裁剪合理，P1 纯净性通过。

## [PROD_NOT_TOUCHED]

本阶段为需求评审，仅读取代码文件和文档，未触碰生产环境。
