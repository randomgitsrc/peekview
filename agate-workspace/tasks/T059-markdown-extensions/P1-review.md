---
phase: P1
task_id: T059
type: review
parent: P1-requirements.md
trace_id: T059-P1-review2-20260720
status: approved
created: 2026-07-20
agent: requirements-review
---

# P1 Review (Round 2): Markdown 扩展补全

## 第一轮问题逐条复核

### 问题 1：6 条 BDD 绑定实现细节（B2/B5/B6/B10/B15/B16）

| BDD | 原问题 | 修订后 Then | 判定 |
|-----|--------|------------|------|
| B2 | 绑定 `class="katex-block"` | "块级公式独占一行渲染，与行内公式视觉区分" | ✅ 已修复，行为描述 |
| B5 | 绑定 `mathcolor 属性` | "错误公式渲染为可见的红色标记文字，不静默消失" | ✅ 已修复，行为描述 |
| B6 | 绑定 `style 属性保留（如 strut 的 height）` | "KaTeX 渲染输出经 DOMPurify 后结构完整，公式视觉正确" | ✅ 已修复，行为描述 |
| B10 | 绑定 `class="task-list-item-checkbox"` | "渲染为带勾选状态的只读 checkbox" | ✅ 已修复，行为描述 |
| B15 | 绑定 `<sup class="footnote-ref">` | "脚注引用渲染为上标链接，指向脚注定义区" | ✅ 已修复，行为描述 |
| B16 | 绑定 `class="footnote-backref"` | "脚注项包含回引链接，点击可跳回引用位置" | ✅ 已修复，行为描述 |

### 问题 2：3 条 BDD Then 不可二值判定（B8/B14/B20）

| BDD | 原问题 | 修订后 Then | 判定 |
|-----|--------|------------|------|
| B8 | "浅色"不可二值 | "公式文字颜色与暗色主题下正文文字颜色一致" | ✅ 已修复，参照标准明确 |
| B14 | "适当"不可二值 | "checkbox 边框和背景与暗色主题下其他表单元素风格一致" | ✅ 已修复，参照标准明确 |
| B20 | "清晰可读"不可二值 | "脚注分隔线、文字、链接颜色与暗色主题下正文风格一致" | ✅ 已修复，参照标准明确 |

### 问题 3：4 条隐含需求缺 BDD（建议 B27-B30）

| BDD | 覆盖维度 | 内容 | 判定 |
|-----|---------|------|------|
| B27 | 边界 | 脚注重复引用渲染正确（含冒号 ID 不导致异常） | ✅ 已新增 |
| B28 | 边界 | 链接文本中 $ 不误触发数学模式 | ✅ 已新增 |
| B29 | 前端 | 块级公式溢出可滚动 | ✅ 已新增 |
| B30 | 前端 | KaTeX 字体加载失败降级 | ✅ 已新增 |

### 问题 4：2.2/2.4 节混入实现方案

| 节 | 原问题 | 修订后内容 | 判定 |
|----|--------|-----------|------|
| 2.2 | 写明 CSS import 路径 | 仅声明"KaTeX CSS 必须在首屏渲染前加载完成，避免 FOUC"+"无需动态加载或按需加载" | ✅ 已修复，纯行为声明 |
| 2.4 | 写明 `scrollIntoView({behavior:'smooth'})` | 仅声明"脚注链接点击后页面滚动到目标位置"+"回引链接同理"+"不应触发 Vue Router 导航" | ✅ 已修复，纯行为声明 |

**注**：2.3 节仍提及 `[data-theme='dark'] .markdown-body .katex` 选择器格式和"覆盖 `.katex` 的 `color` 即可"，以及 2.4 节提及 `overflow:auto` 容器和 `scrollIntoView` 模式——这些属于现状描述（解释为何存在隐含需求），而非规定实现方案。2.3 说明的是 KaTeX CSS 的颜色继承机制（事实），2.4 说明的是当前页面已有的滚动模式（事实）。不构成 P1 纯净性问题。

## BDD 全量评审

### B1 — 行内公式渲染
- **判定**：PASS（可二值）
- **覆盖维度**：数据✓ 前端✓ 多端✗ 边界✗ 兼容✓

### B2 — 块级公式渲染
- **判定**：PASS（可二值）
- **覆盖维度**：数据✓ 前端✓ 多端✗ 边界✗ 兼容✓

### B3 — 货币符号不误触发
- **判定**：PASS（可二值）
- **覆盖维度**：数据✓ 前端✓ 多端✗ 边界✓ 兼容✓

### B4 — 未闭合分隔符降级
- **判定**：PASS（可二值）
- **覆盖维度**：数据✓ 前端✓ 多端✗ 边界✓ 兼容✓

### B5 — KaTeX 错误公式可见
- **判定**：PASS（可二值）
- **覆盖维度**：数据✓ 前端✓ 多端✗ 边界✓ 兼容✗

### B6 — DOMPurify 不剥离 KaTeX 输出
- **判定**：PASS（可二值）
- **覆盖维度**：数据✓ 前端✓ 多端✗ 边界✗ 兼容✓

### B7 — KaTeX CSS 已加载
- **判定**：PASS（可二值）
- **覆盖维度**：数据✗ 前端✓ 多端✗ 边界✗ 兼容✓

### B8 — 暗色模式下公式可见
- **判定**：PASS（可二值）
- **覆盖维度**：数据✗ 前端✓ 多端✗ 边界✗ 兼容✓

### B9 — 行内代码中 $ 不触发数学
- **判定**：PASS（可二值）
- **覆盖维度**：数据✓ 前端✓ 多端✗ 边界✓ 兼容✓

### B10 — 已完成任务渲染
- **判定**：PASS（可二值）
- **覆盖维度**：数据✓ 前端✓ 多端✗ 边界✗ 兼容✓

### B11 — 未完成任务渲染
- **判定**：PASS（可二值）
- **覆盖维度**：数据✓ 前端✓ 多端✗ 边界✗ 兼容✓

### B12 — DOMPurify 不剥离 checkbox
- **判定**：PASS（可二值）
- **覆盖维度**：数据✓ 前端✓ 多端✗ 边界✗ 兼容✓

### B13 — checkbox 不可交互
- **判定**：PASS（可二值）
- **覆盖维度**：数据✗ 前端✓ 多端✗ 边界✗ 兼容✓

### B14 — 暗色模式下 checkbox 可见
- **判定**：PASS（可二值）
- **覆盖维度**：数据✗ 前端✓ 多端✗ 边界✗ 兼容✓

### B15 — 脚注引用渲染
- **判定**：PASS（可二值）
- **覆盖维度**：数据✓ 前端✓ 多端✗ 边界✗ 兼容✓

### B16 — 脚注回引链接
- **判定**：PASS（可二值）
- **覆盖维度**：数据✓ 前端✓ 多端✗ 边界✗ 兼容✓

### B17 — 未定义脚注降级
- **判定**：PASS（可二值）
- **覆盖维度**：数据✓ 前端✓ 多端✗ 边界✓ 兼容✓

### B18 — 脚注锚点点击滚动
- **判定**：PASS（可二值）
- **覆盖维度**：数据✗ 前端✓ 多端✗ 边界✗ 兼容✓

### B19 — 脚注回引点击滚动
- **判定**：PASS（可二值）
- **覆盖维度**：数据✗ 前端✓ 多端✗ 边界✗ 兼容✓

### B20 — 暗色模式下脚注可读
- **判定**：PASS（可二值）
- **覆盖维度**：数据✗ 前端✓ 多端✗ 边界✗ 兼容✓

### B21 — 上标渲染
- **判定**：PASS（可二值）
- **覆盖维度**：数据✓ 前端✓ 多端✗ 边界✗ 兼容✓

### B22 — 下标渲染
- **判定**：PASS（可二值）
- **覆盖维度**：数据✓ 前端✓ 多端✗ 边界✗ 兼容✓

### B23 — 上标在加粗内正常
- **判定**：PASS（可二值）
- **覆盖维度**：数据✓ 前端✓ 多端✗ 边界✓ 兼容✓

### B24 — 空分隔符降级
- **判定**：PASS（可二值）
- **覆盖维度**：数据✓ 前端✓ 多端✗ 边界✓ 兼容✓

### B25 — 多扩展共存无冲突
- **判定**：PASS（可二值）
- **覆盖维度**：数据✓ 前端✓ 多端✗ 边界✗ 兼容✓

### B26 — 现有功能不受影响
- **判定**：PASS（可二值）
- **覆盖维度**：数据✓ 前端✓ 多端✗ 边界✓ 兼容✓

### B27 — 脚注重复引用渲染正确
- **判定**：PASS（可二值）
- **覆盖维度**：数据✓ 前端✓ 多端✗ 边界✓ 兼容✓

### B28 — 链接文本中 $ 不误触发数学模式
- **判定**：PASS（可二值）
- **覆盖维度**：数据✓ 前端✓ 多端✗ 边界✓ 兼容✓

### B29 — 块级公式溢出可滚动
- **判定**：PASS（可二值）
- **覆盖维度**：数据✗ 前端✓ 多端✗ 边界✓ 兼容✓

### B30 — KaTeX 字体加载失败降级
- **判定**：PASS（可二值）
- **覆盖维度**：数据✗ 前端✓ 多端✗ 边界✓ 兼容✓

## 隐含需求覆盖

### 数据维度
- **覆盖**：B3/B4/B9/B17/B24/B27/B28 覆盖边界输入
- **遗漏**：无新增遗漏。第一轮提出的"脚注重复引用"已由 B27 覆盖；"链接内 $" 已由 B28 覆盖

### 前端维度
- **覆盖**：暗色模式（B8/B14/B20）、CSS 加载（B7）、滚动交互（B18/B19）、只读性（B13）、溢出（B29）、字体降级（B30）
- **遗漏**：无

### 多端维度
- **覆盖**：2.8 节确认无后端/MCP/CLI 影响
- **遗漏**：无（纯前端任务，多端维度不适用）

### 边界维度
- **覆盖**：货币冲突（B3）、未闭合（B4）、错误公式（B5）、空分隔符（B24）、未定义脚注（B17）、重复引用含冒号 ID（B27）、链接内 $（B28）
- **遗漏**：无

### 兼容维度
- **覆盖**：现有功能不受影响（B26）、DOMPurify 兼容（B6/B12）、HTML render 路径无影响（2.8 节声明）
- **遗漏**：无

## 裁剪评审

- **P3 保留（简化）**：理由充分
- **P7 保留**：理由充分
- **P8 保留**：理由充分
- **所有阶段保留**：合理，无不当裁剪

## 评审结论

**status: approved**

所有第一轮问题已修复：
- B1-B30 全部可二值判定
- 6 条实现细节绑定已改为行为描述
- 3 条不可判定 Then 已量化参照标准
- 4 条新增 BDD（B27-B30）覆盖隐含需求
- 2.2/2.4 节实现方案已移除，仅保留行为声明

覆盖维度清单：数据✓ 前端✓ 多端✓（不适用） 边界✓ 兼容✓
