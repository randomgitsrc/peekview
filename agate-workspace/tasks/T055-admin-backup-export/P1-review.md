---
phase: P1
task_id: T055
type: review
parent: P1-requirements.md
trace_id: T055-P1-review2-20260716
status: approved
created: 2026-07-16
agent: requirements-review
---

# T055 P1 Requirements Review (Second Review)

## R1-R15 解决状态

| # | 严重度 | 问题 | 修订后状态 | 验证锚点 |
|---|--------|------|-----------|----------|
| R1 | HIGH | BDD-01 "不阻塞正常读写操作"不可二值判定 | **已解决** | BDD-01 已删除该条款，一致性保证移至 P5 技术验证 |
| R2 | HIGH | SHA256 校验和覆盖范围模糊 | **已解决** | BDD-01 Then 子句明确"metadata.json 的 file_checksums 字段列出 tar.gz 内除 metadata.json 外每个文件的 SHA256"；BDD-04 同步 |
| R3 | HIGH | BDD-16 回滚定义不完整 | **已解决** | BDD-16 现为"DB 事务回滚 + 已复制文件被清理 + 不留下半完成结果"三段式定义 |
| R4 | MEDIUM | BDD-10 "搜索功能正常"不可二值判定 | **已解决** | BDD-10 改为"对备份中某 entry 的 summary 关键词搜索返回该 entry" |
| R5 | MEDIUM | BDD-11 缺同版本/低版本 BDD | **已解决** | 新增 BDD-11a（同版本允许）+ BDD-11b（低版本允许+提示） |
| R6 | MEDIUM | BDD-12 slug 重命名无限递归 | **已解决** | BDD-12 明确"{slug}-{n}，n 从 1 递增直到唯一" |
| R7 | MEDIUM | BDD-13 命令笔误 | **已解决** | 已修正为 `peekview admin restore` |
| R8 | MEDIUM | BDD-15 dry-run 输出格式未明确 | **已解决** | BDD-15 列出具体字段：entry_count, user_count, api_key_count, conflicts[], version_check |
| R9 | MEDIUM | BDD-17 "或"不可二值判定 | **已解决** | BDD-17 改为"备份文件默认输出到当前工作目录" |
| R10 | MEDIUM | entry_shares/entry_reads 未在 BDD-10 验证 | **已解决** | BDD-10 Given/Then 均列出 entry_shares、entry_reads |
| R11 | LOW | BDD-01 掺入 ".backup API" | **已解决** | 改为"一致性快照，非文件系统级拷贝" |
| R12 | LOW | I7 掺入 "WAL checkpoint" | **已解决** | I7 改为"备份须包含 WAL 中尚未写入主 DB 文件的数据" |
| R13 | LOW | BDD-02 缺输出路径边界条件 | **已解决** | BDD-02 补充"若路径已存在同名文件则覆盖"+"若父目录不存在或无写权限，命令报错退出" |
| R14 | LOW | risk_level 偏低 | **已解决** | 调为 medium-high，补充 restore 破坏性 + FK 链复杂度 + 文件/DB 非原子性理由 |
| R15 | LOW | export remote 模式行为未定义 | **已解决** | C2 给出默认立场："初版仅支持 local 模式，export 遇 remote 配置时报错退出" |

**结论：R1-R15 全部已解决，无遗留。**

## BDD 评审（逐条二值判定检查）

### BDD-01: 一致性备份
- **可二值判定**: ✓（tar.gz 包含指定文件列表 + metadata.json 含 file_checksums = PASS）
- **覆盖维度**: 数据✓ 前端✗(合理) 多端✗ 边界✓(空实例由BDD-05覆盖) 兼容✓

### BDD-02: 备份自定义输出路径
- **可二值判定**: ✓（文件存在/报错退出 = PASS/FAIL）
- **覆盖维度**: 数据✓ 前端✗ 多端✗ 边界✓(覆盖/不存在/无权限) 兼容✓

### BDD-03: 备份拒绝 remote 模式
- **可二值判定**: ✓（退出码非0 + 无文件 = PASS）
- **覆盖维度**: 数据✓ 前端✗ 多端✓ 边界✓ 兼容✓

### BDD-04: 备份完整性
- **可二值判定**: ✓（SHA256 逐文件匹配 = PASS）
- **覆盖维度**: 数据✓ 前端✗ 多端✗ 边界✓(篡改检测) 兼容✓

### BDD-05: 空实例备份
- **可二值判定**: ✓
- **覆盖维度**: 数据✓ 前端✗ 多端✗ 边界✓(空数据) 兼容✓

### BDD-06: 单条目 JSON 导出
- **可二值判定**: ✓（JSON 结构 + 字段存在/类型 = PASS）
- **覆盖维度**: 数据✓ 前端✗ 多端✗ 边界✗(缺：无文件entry、空文件) 兼容✓
- **备注**: 无文件 entry 的 JSON 导出（files 数组为空）未显式覆盖，但 BDD-06 的 files 数组定义隐含了空数组场景。非阻塞。

### BDD-07: 单条目 ZIP 导出
- **可二值判定**: ✓
- **覆盖维度**: 数据✓ 前端✗ 多端✗ 边界✗(缺：二进制文件在 ZIP 中的处理) 兼容✓
- **备注**: "每个文件以原始路径存储在 ZIP 中"——"原始路径"是否含 entry_id/ 前缀未明确。P2 设计时应定义 ZIP 内目录结构。非阻塞（P2 可补全）。

### BDD-08: 导出不存在的 entry
- **可二值判定**: ✓
- **覆盖维度**: 数据✓ 前端✗ 多端✗ 边界✓ 兼容✓

### BDD-09: 导出默认格式
- **可二值判定**: ✓
- **覆盖维度**: 数据✓ 前端✗ 多端✗ 边界✗ 兼容✓

### BDD-10: 基本恢复（空目标实例）
- **可二值判定**: ✓（数据存在 + 文件可访问 + FTS 搜索返回 = PASS）
- **覆盖维度**: 数据✓ 前端✗ 多端✗ 边界✗(缺：恢复后密码验证) 兼容✓
- **备注**: 恢复后用户密码是否仍可登录未显式验证。但 I1 隐含需求已覆盖 .secret_key 备份，BDD-10 验证了 JWT 密钥文件恢复，bcrypt hash 是跨版本兼容的。非阻塞。

### BDD-11: 恢复版本兼容——高版本拒绝
- **可二值判定**: ✓（报错退出 + 数据不变 = PASS）
- **覆盖维度**: 数据✓ 前端✗ 多端✗ 边界✓ 兼容✓

### BDD-11a: 恢复版本兼容——同版本允许
- **可二值判定**: ✓（恢复成功 = PASS）
- **覆盖维度**: 数据✓ 前端✗ 多端✗ 边界✗ 兼容✓

### BDD-11b: 恢复版本兼容——低版本允许
- **可二值判定**: ✓（恢复成功 + 提示 = PASS）
- **覆盖维度**: 数据✓ 前端✗ 多端✗ 边界✗ 兼容✓

### BDD-12: 恢复 ID 冲突处理（merge 模式）
- **可二值判定**: ✓（新ID不冲突 + slug重命名 + FK映射 + 目录名一致 = PASS）
- **覆盖维度**: 数据✓ 前端✗ 多端✗ 边界✓(冲突) 兼容✓

### BDD-13: 恢复拒绝 remote 模式
- **可二值判定**: ✓
- **覆盖维度**: 数据✓ 前端✗ 多端✓ 边界✓ 兼容✓

### BDD-14: 恢复完整性验证
- **可二值判定**: ✓（校验失败报错 = PASS）
- **覆盖维度**: 数据✓ 前端✗ 多端✗ 边界✓ 兼容✓

### BDD-15: 恢复 dry-run
- **可二值判定**: ✓（输出含指定字段 + 不修改数据 = PASS）
- **覆盖维度**: 数据✓ 前端✗ 多端✗ 边界✓ 兼容✓

### BDD-16: 恢复中断安全
- **可二值判定**: ✓（DB 不变 + 文件目录不变 + 无半完成结果 = PASS）
- **覆盖维度**: 数据✓ 前端✗ 多端✗ 边界✓(中断) 兼容✓

### BDD-17: debug 模式隔离
- **可二值判定**: ✓（数据来源 = /tmp/peekview-debug/ + 输出到 CWD = PASS）
- **覆盖维度**: 数据✓ 前端✗ 多端✗ 边界✓ 兼容✓

## 隐含需求覆盖

### 数据维度
- **覆盖**: I1(.secret_key/.captcha_secret) ✓ I2(metadata.json) ✓ I3(slug冲突) ✓ I4(FTS5重建) ✓ I5(目录重命名) ✓ I7(WAL数据) ✓ I8(FK依赖顺序) ✓ I9(完整性校验) ✓
- **遗漏**: 无新增遗漏。首次 review M1(entry_shares/entry_reads)已通过 BDD-10 补充解决。M2(密码hash)和 M3(expires_at)为低优先级边界场景，P2/P4 可覆盖。

### 前端维度
- **覆盖**: 合理不覆盖（纯 CLI 改动，ui_affected: false）

### 多端维度
- **覆盖**: remote 模式拒绝（BDD-03, BDD-13）✓ export 仅 local（C2 已给默认立场）✓

### 边界维度
- **覆盖**: 空实例(BDD-05) ✓ 不存在entry(BDD-08) ✓ ID/slug冲突(BDD-12) ✓ 中断安全(BDD-16) ✓ 输出路径边界(BDD-02) ✓
- **遗漏**: M5(超大备份OOM)和 M6(备份期间并发写入的文件/DB一致性)未新增 BDD。capability_requirements 已声明 tarfile 流式写入能力，M6 属于 P5 技术验证范畴。非阻塞。

### 兼容维度
- **覆盖**: 高版本拒绝(BDD-11) ✓ 同版本允许(BDD-11a) ✓ 低版本允许(BDD-11b) ✓ 不破坏现有命令(Section 2.2) ✓
- **遗漏**: M8(无 metadata.json 的旧格式备份)未覆盖。但 metadata.json 是本次新增格式，不存在"旧格式无 metadata.json"的场景——首次发布不存在向后兼容问题。非阻塞。

## 裁剪评审

### P7 裁剪
- **理由**: "纯后端 CLI 改动，无前端/MCP 影响，无跨包一致性风险"
- **判定**: 合理。packages 仅 backend/peekview/，无跨包版本同步需求。

### P6 简化
- **理由**: "CLI 命令，pytest 验证即可，无需 Playwright"
- **判定**: 合理。纯 CLI 无需浏览器验证。

### risk_level: medium-high
- **判定**: 合理。修订后补充了 restore 破坏性 + FK 链复杂度 + 文件/DB 非原子性理由，与实际风险匹配。

## P1 纯净性

### 解决方案设计掺入检查
1. BDD-01: "一致性快照，非文件系统级拷贝"——描述需求性质，未指定实现方式。✓
2. I7: "备份须包含 WAL 中尚未写入主 DB 文件的数据"——描述需求，未指定 checkpoint。✓
3. capability_requirements: need 名称改为"sqlite-consistent-backup"，available 列出能力来源（标准库/SQLAlchemy）而非具体 API 调用方式。✓

**无新增解决方案设计掺入。**

## 修订引入新问题检查

逐项检查修订内容是否引入新问题：

1. BDD-01 新增"metadata.json 的 file_checksums 字段列出 tar.gz 内除 metadata.json 外每个文件的 SHA256"——与 BDD-04 一致，无冲突。
2. BDD-02 新增"若路径已存在同名文件则覆盖"——覆盖行为明确，无歧义。
3. BDD-11a/11b 新增——编号合理，与 BDD-11 形成完整版本兼容矩阵。
4. BDD-12 slug 重命名规则"{slug}-{n}"——规则明确，可二值判定。
5. BDD-15 dry-run 字段列表——字段明确，可二值判定。
6. BDD-16 三段式回滚定义——每段可独立判定。
7. BDD-17 "当前工作目录"——明确，可二值判定。
8. risk_level 调为 medium-high——合理。
9. C2 给出默认立场——消除待确认项的不确定性。

**未发现修订引入的新问题。**

## 评审结论

**status: approved**

所有 HIGH 问题（R1-R3）已解决，所有 MEDIUM 问题（R4-R10）已解决，所有 LOW 问题（R11-R15）已解决。修订未引入新问题。所有 BDD 条件可二值判定。P1 纯净性无掺入。

BDD 编号锚点：
- BDD-01: ✓ 可二值判定（R1,R2,R11 已解决）
- BDD-02: ✓ 可二值判定（R13 已解决）
- BDD-03: ✓ 可二值判定
- BDD-04: ✓ 可二值判定（R2 已解决）
- BDD-05: ✓ 可二值判定
- BDD-06: ✓ 可二值判定
- BDD-07: ✓ 可二值判定（ZIP 内路径细节留 P2）
- BDD-08: ✓ 可二值判定
- BDD-09: ✓ 可二值判定
- BDD-10: ✓ 可二值判定（R4,R10 已解决）
- BDD-11: ✓ 可二值判定（R5 已解决，11a/11b 补充）
- BDD-11a: ✓ 可二值判定
- BDD-11b: ✓ 可二值判定
- BDD-12: ✓ 可二值判定（R6 已解决）
- BDD-13: ✓ 可二值判定（R7 已解决）
- BDD-14: ✓ 可二值判定
- BDD-15: ✓ 可二值判定（R8 已解决）
- BDD-16: ✓ 可二值判定（R3 已解决）
- BDD-17: ✓ 可二值判定（R9 已解决）

覆盖维度总结：
- 数据: ✓ 完整（I1-I5,I7-I9 全覆盖，entry_shares/entry_reads 已补充）
- 前端: ✓ 合理不覆盖（纯 CLI）
- 多端: ✓ 完整（remote 拒绝 + export local-only 立场）
- 边界: ✓ 基本完整（空/冲突/中断/路径边界；超大文件/并发一致性属 P5 范畴）
- 兼容: ✓ 完整（高/同/低版本三场景覆盖）
