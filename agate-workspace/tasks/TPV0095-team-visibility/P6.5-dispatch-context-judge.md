---
phase: P6.5
task_id: TPV0095
type: judge
parent: P6-acceptance.md
trace_id: TPV0095-P6.5-judge-20260902
status: draft
agent: judge
---

# P6.5 派发上下文 — judge 独立复核（TPV0095 team-visibility）

<!-- AGATE_CARD_START -->
<!-- AGATE_CARD_END -->

<dispatch_guide>

### 目标

独立复核 TPV0095 的 P6 验收结论：fresh context 逐条重验**所有 44 条 BDD**（含已 PASS 项，零挑验），只信证据与 git log，产出 `P6.5-judge-verdict.md`。

### Judge 信息隔离（强制）

**你的输入只传路径，且只允许白名单**：
- P1-requirements.md / P2-design.md（仅验收相关节）/ P6-evidence/ 目录 / .state.yaml / gate-events.jsonl（另授 git log 查询权）
- **禁止输入（黑名单，禁读）**：P6-acceptance.md、P6/P5/P4-dispatch-context-*.md、P4-implementation.md、P4-review*.md、P5-test-results/——verifier/implementer 的自述与派发上下文一律不读

### 认知约束（只信证据与 git log）

1. 逐条重验所有 BDD：每条结论必须引用 P6-evidence/ 下真实存在且非空的证据文件
2. 禁止"看起来没问题"式结论
3. git log 可查证执行留痕（P5/P6 commit 存在性等）
4. 三档预算：轮次 ≤2 / 时间 30min。预算耗尽 → 按已验条目落盘 verdict，status: needs-revision + partial: true

### verdict 产出格式

`P6.5-judge-verdict.md`：
- Header: phase=P6.5 / status（passed/rejected/needs-revision）+ criteria_total（= P1 #### BDD-NN 标题数 = 44）+ criteria_passed + verdict_evidence [JSON 列表]（+ partial 可选）
- 正文每条 BDD 一行：`- (PASS|FAIL|NEEDS-REVISION) BDD-NN: 描述 (证据路径)`

### 输入文件（白名单）

1. `P1-requirements.md`（44 BDD 权威）
2. `P2-design.md`（仅验收相关节，如 §11 实现完成标志）
3. `P6-evidence/`（backend/ + frontend/ + screenshots/ + vision-reports/）
4. `.state.yaml`（judge.enabled 确认）
5. git log（执行留痕查证）

### 产出

- `P6.5-judge-verdict.md`（status + criteria_total/criteria_passed + 44 行判定）

### 返回

两行：① P6.5-judge-verdict.md 路径；② Status + criteria 摘要（≤40 字）。
</dispatch_guide>
