===== bdd39 =====
基于您提供的 PeekView explore 卡片网格截图，以下是针对这 4 个问题的逐项回答：

1. **含 badge 的 entry 卡片**：
   * 网格中包含 `badge` 的卡片有两张：
     * 左下角区域的 **"team badge 1788380666462"**
     * 右上角区域的 **"team badge 1788380666979"**

2. **badge 文字是否含「仅团队可见 · PV6-…」**：
   * 这两张 `badge` 卡片上显示的可见性标签文字均 **不包含** `PV6-...`。
   * 它们的标签文字是：**「仅团队可见 · Proj A」**（包含中文字符和项目名称 `Proj A`，而非 `PV6-` 前缀的编号）。

3. **卡片上是否有 Private/private 字样**：
   * 卡片上 **没有** 出现英文单词 "Private" 或 "private"。
   * 系统统一使用了中文标签 **「仅团队可见」** 来标识私密/团队状态。

4. **卡片操作区有哪些按钮图标（如星标/复制/删除/可见性切换）**：
   * **当前截图中的卡片表面** 仅显示了标题、作者（如 `@alice` / `@bob`）、更新时间（`8h ago`）、文件数（`1 file`）以及底部的可见性/项目标签（如 `仅团队可见 · Proj A`）。
   * 在当前默认状态下，卡片上 **没有直接显示** 星标、复制、删除或可见性切换等操作图标（这些悬停操作按钮在当前静态截图中未浮现）。
[exit 0]
