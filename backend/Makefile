# ⚠️ 此文件已废弃。所有操作请使用项目根目录的 Makefile。
# 保留以下 target 仅为兼容旧 muscle memory。

.PHONY: dev test test-cov lint build clean publish

dev test test-cov lint build clean publish:
	@echo "⚠️  请使用根目录 Makefile: cd .. && make $@"
	@exit 1
