# 安全审计报告 2026 Q3

**审计人**：Agent-security-01
**日期**：2026-07-25
**范围**：PeekView v1.1.0 全栈

## 审计结果

| 级别 | 数量 | 状态 |
|------|------|------|
| 严重 | 0 | — |
| 高危 | 1 | 已修复 |
| 中危 | 3 | 2 已修复，1 观察中 |
| 低危 | 5 | 记录待处理 |

## 已修复项

1. **[高危] 路径遍历**：`storage.py` 未校验 `..`，已增加 `resolve()` 后校验是否在 `data_dir` 内
2. **[中危] FTS5 注入**：搜索查询未转义引号，已增加净化逻辑
3. **[中危] API key 日志泄露**：错误日志中打印了完整 key，已脱敏为 `pv_****1234`

## 观察中

- **[中危] CSP unsafe-eval**：Mermaid/d3 依赖 `new Function()`，暂不可移除。已通过 `sandbox` iframe 隔离缓解

## 建议

- 下季度引入自动化 SAST 扫描到 CI
- 定期轮换 JWT secret
